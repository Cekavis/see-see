# Data Model: Tray Click Settings

This feature introduces no stored data, entities, relationships, validation rules, or state transitions.

The only transient input is a tray mouse event containing a button and button state.

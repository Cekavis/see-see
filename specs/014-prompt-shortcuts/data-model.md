# Data Model
PromptPreset gains nullable capture shortcut. App settings no longer stores active prompt or global capture shortcut. Shortcut registration maps each configured shortcut to its prompt id.

# Validation Record: Result Window Size Memory

**Date**: 2026-09-16

The preview limit was changed from 100px to 60px at the user's request. The user explicitly requested that no further automated or manual validation be run after that change.

The signed Windows 0.15.2 package was rebuilt and installed locally so the delivered package contains the 60px preview limit.
